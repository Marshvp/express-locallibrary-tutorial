extends layout

block content
  h1= title

  if author_list.length
    ul
      each author in author_list
        li
          a(href=author.url) #{author.name}
          |  (#{author.date_of_birth_formated} - 
          if author.date_of_death != null
            | #{author.date_of_death_formated})
          else
            | present)
  else
    p There are no authors.

