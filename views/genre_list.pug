extends layout

block content
    h1= title

    if genre_list.length
        ul 
            each genre in genre_list
                li
                    a(href=genre.url) #{genre.name}

        p There are !{genre_list.length} genres.